version: '3'
services:

  nginx:
    hostname: ${HOSTNAME}
    environment:
      VIRTUAL_HOST: ${HOSTNAME},${HOSTNAME2},${HOSTNAME3},${HOSTNAME4},${HOSTNAME5}
      NGINX_HOST: ${HOSTNAME}
    expose:
      - 80
      - 443
    volumes:
      - "./inc/default.conf:/etc/nginx/conf.d/default.conf"
      - "./openrsc_web:/var/www/html/openrsc_web"
      - "./log:/var/log/nginx"
      - "./avatars:/var/www/html/openrsc_web/public/img/avatars"
      - "./downloads:/var/www/html/openrsc_web/public/downloads"

  php:
    environment:
      - XDEBUG=false

networks:
  default:
    external:
      name: nginx-proxy
